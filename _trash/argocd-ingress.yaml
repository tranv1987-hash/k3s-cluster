apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-ingress
  namespace: argocd
  annotations:
    cert-manager.io/cluster-issuer: selfsigned-issuer
    # CRITICAL CHANGE: Tells Traefik the backend is HTTPS
    traefik.ingress.kubernetes.io/service.serversscheme: https
spec:
  ingressClassName: traefik  # <--- CHANGED: Matches your K3s default
  tls:
  - hosts:
    - argocd.192.168.30.190.nip.io  # <--- CHANGED: Points to Traefik (.190)
    secretName: argocd-server-tls
  rules:
  - host: argocd.192.168.30.190.nip.io  # <--- CHANGED: Points to Traefik (.190)
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              number: 443
